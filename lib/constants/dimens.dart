// drawer width on desktop (when open)
const double sideBarDesktopWidth = 300;

// drawer width on tablet when closed
const double sideBarTabletWidth = 80;


// icon details width on desktop (when open)
const double iconDetailsWidth = 350;